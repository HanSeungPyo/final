<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MemberMapper">
	<select id="login" resultType="com.finals.member.MemberDTO">
		select * from MEMBER where ID =#{ID} and PW = #{PW}
	</select>
	<insert id="join">
		insert into MEMBER(id, pw, name, gender, year, month, day, email, phone, pum1, pum2, address) values(#{id},#{pw},#{name},#{gender},#{year},#{month},#{day},#{email},#{phone},#{pum1},#{pum2},#{address})
	</insert>
</mapper>